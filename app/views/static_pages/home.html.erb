<% provide(:title, "Home") %>

<% if logged_in? %>
	<div>
		<div id="cor">
			<h3>Ultimas Ofertas</h3>
			<%= render 'shared/feed' %>
		</div>
		<div  class="container" id="core">
			<%= render 'shared/feed' %>
		</div>
	</div>

<% else %>

	<div class="center jumbotron">
		<h1>Bem vindo á Bolsa de Emprego</h1>
		<h2>
			Essa é a Bolsa de empregos
		</h2>
	</div>
<% end %>
