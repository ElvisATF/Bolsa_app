<%= form_with(model: current_user_user.active_relationships.build, local: true) do |f| %>

<div><%= hidden_field_tag :followed_id, @user_entity.id %></div>

<%= f.submit "Follow", class: "btn btn-primary" %>

<% end %>
